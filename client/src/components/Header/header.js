import React from "react";

const Header = () => (
  <nav className="navbar navbar-dark bg-primary">
    <div className="container">
      <span className="navbar-brand mb-0 h1">Elder Quiz</span>
    </div>
  </nav>
);

export default Header;
